import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.mobidashboard',
  appName: 'MobiDashboard',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
